import { LimitCharactersDirective } from './limit-characters.directive';

describe('LimitCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
